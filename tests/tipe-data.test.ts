describe('data type', () => { 
    it('sould be declare', () => {
        const name: string = "eko kurniawan khanedy"
        const balance: number = 100000
        const isVip: boolean = true

        console.log(name)
        console.log(balance)
        console.log(isVip)
    })
 })