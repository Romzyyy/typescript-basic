"use strict";
describe('data type', () => {
    it('sould be declare', () => {
        const name = "eko kurniawan khanedy";
        const balance = 100000;
        const isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
    });
});
